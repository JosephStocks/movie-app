
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="Content-Security-Policy" content="default-src *;
    img-src * 'self' data: https:; script-src 'self' 'unsafe-inline' 'unsafe-eval' *;
    style-src  'self' 'unsafe-inline' *">
    <title>The Movie Matrix</title>
    <link rel="stylesheet" href="/css/tailwind.css">
    <link rel="stylesheet" href="/css/main.css">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Libre+Barcode+39+Extended+Text&display=swap" rel="stylesheet">
</head>
<body class="bg-black">
        
    <%- include ('partials/navbar.ejs') %> 

    <div class="text-white text-center">Top 100 List | ID: <%= genreid %> </div>

    <div class="flex flex-row flex-wrap">
        <% movieArr.forEach(movie => { %>
            
            <div class="container mx-auto bg-black py-10 px-5 flex justify-center items-center">
                <button class="modal-open">
                    <div class="bg-black w-50 shadow-lg cursor-pointer transform hover:scale-105 duration-300 ease-in-out overflow-hidden rounded-lg">
                        <div class="">
                            <img src="https://image.tmdb.org/t/p/w500<%= movie.poster_rel_url %> " alt="" class="w-full">
                        </div>
                        <div class="p-1 flex flex-col justify-center items-center h-24">
                            <p class="block text-sm md:text-2xl uppercase font-mono text-white truncate"><%= movie.title %></p>
                            <p class="block text-xs med:text-sm uppercase font-mono text-white">(<%= movie.year %>)</p>
                        </div>
                    </div>
                </button>
            </div>
        <% }) %>
    </div>

    
    <script src="/js/modal.js"></script>
    <script src="/js/login-dropdown.js"></script>

</body>
</html>
